<?xml version="1.0" encoding="utf-8"?>
<!--
    Themed ("monochrome") icons are tinted from the alpha channel alone, so the colour mark cannot serve
    here: it is a solid disc and would theme as a plain circle. This is the mark reduced to its silhouette
    — an open ring, the O with a gap.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#00000000"
        android:pathData="M54,28 A26,26 0 1 1 31.48,41"
        android:strokeColor="#FFFFFF"
        android:strokeLineCap="round"
        android:strokeWidth="9" />
</vector>
